# Reference questions (intake)

Drop your existing platform question zips or questions.json files here.
Claude reads them from this folder and extracts clean gold *config* examples
into knowledge/examples/code_editor_type/ (so the design agent learns your
house style).

You can delete files here after extraction — they are only an input.
